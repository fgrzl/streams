package server

const (
	// Prefixes all keys in the database or either (DATA, INV, TRX)
	DATA        = "DAT"
	INVENTORY   = "INV"
	TRANSACTION = "TRX"

	// Additional key parts
	SPACES   = "SPACES"
	SEGMENTS = "SEGMENTS"
)
